{
  "private": true,
  "name": "ghost-cache-purge",
  "version": "1.0.0",
  "description": "A Cloudflare Worker to purge cached pages when a post is updated.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "milgradesec <38557680+milgradesec@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
